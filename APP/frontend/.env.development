# Loaded automatically by `next dev`. Not used by production builds.
# TEMP-DEV-SIMULATION: remove this block to hide simulate buttons (license, passport OCR, payment).
# Backend still requires DIAMOND_SIMULATION_ENABLED=true.
NEXT_PUBLIC_DIAMOND_SIMULATION=true
